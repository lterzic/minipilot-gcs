{
    "name": "minipilot-gcs",
    "private": true,
    "workspaces": [
        "packages/proto",
        "packages/sim-gateway",
        "packages/frontend"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "clean": "npm run clean --workspaces && rm -rf build",
        "start": "npm run build && npm run start --workspaces"
    }
}