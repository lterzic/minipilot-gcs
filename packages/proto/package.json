{
    "private": true,
    "name": "@minipilot-gcs/proto",
    "description": "Generated TypeScript definitions from minipilot-proto messages",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "type": "module",
    "scripts": {
        "build": "make build && tsc",
        "clean": "make clean && rm -rf dist"
    },
    "dependencies": {
        "ts-proto": "^2.7.5"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}
