{
    "private": true,
    "name": "@minipilot-gcs/backend",
    "description": "Receive messages from minipilot and provide access points for (or push to) the frontend",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist",
        "start": "node dist/index.js"
    },
    "dependencies": {
        "@minipilot-gcs/proto": "*",
        "ws": "^8.14.2"
    },
    "devDependencies": {
        "@types/node": "^20.9.0",
        "@types/ws": "^8.5.8",
        "typescript": "^5.2.2"
    }
}